<% if study_log.errors.any? %>
  <div style="color: red;">
    <ul>
      <% study_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: study_log, local: true do |f| %>
  <div>
    <%= f.label :title, 'タイトル' %><br>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :content, '内容' %><br>
    <%= f.text_area :content %>
  </div>

  <div>
    <%= f.submit study_log.persisted? ? '更新する' : '投稿する' %>
  </div>
<% end %>
